<view class="container">
  <view class="info-card">
    <text class="info-title">AI 云端网关</text>
    <text class="info-desc">Coze 与讯飞密钥只在 aiRouter 云函数环境变量中配置，小程序端不保存任何外部 API 密钥。</text>
    <text class="help-link" bindtap="showHelp">查看云端配置说明 ></text>
  </view>

  <view class="config-card">
    <view class="card-header">
      <text class="card-title">Bot ID 配置</text>
    </view>
    <view class="input-group" wx:for="{{bots}}" wx:for-item="botId" wx:for-index="botType" wx:key="botType">
      <text class="input-label">{{botLabels[botType]}}</text>
      <input
        class="input-field"
        type="text"
        placeholder="请输入 Bot ID"
        value="{{botId}}"
        data-type="{{botType}}"
        bindinput="onBotInput"
      />
    </view>
  </view>

  <view class="button-group">
    <button class="btn-test" bindtap="testConnection">测试云端网关</button>
    <button class="btn-save" bindtap="saveConfig">保存 Bot ID</button>
  </view>

  <view class="tips">
    <text class="tips-text">提示：Token、APPID、APIKey、APISecret 均不会写入本地缓存。</text>
  </view>
</view>
